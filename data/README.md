# Data Directory

- `discovery/`: captured HTML, JS chunks, API samples, and discovery reports.
- `raw/`: immutable weekly AH API payloads.
- `processed/`: normalized datasets for the recipe app.

Do not commit raw weekly data by default. Keep small discovery samples only when they are useful for development.
